# ⚛️ Mini React Engine

A minimal React-like library built from scratch in vanilla JavaScript to demonstrate how **Virtual DOM, reconciliation, and DOM updates** work internally.

This project recreates the **core ideas behind React** without any external libraries.

---

## 🚀 What This Project Does

This library implements:

- Virtual DOM
- JSX-like `createElement`
- Text nodes
- DOM rendering
- Diffing (Reconciliation)
- DOM updates
- Re-rendering on state change

It helps developers understand **how React actually works internally**.

---

## 📁 Project Structure

